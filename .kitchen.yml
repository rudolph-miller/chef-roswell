---
driver:
  name: docker
  socket: <%= ENV['DOCKER_HOST'] %>

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu
  - name: centos

suites:
  - name: default
    run_list:
      - recipe[roswell]
    attributes:
      roswell:
        user: "kitchen"
        version: "sbcl"
